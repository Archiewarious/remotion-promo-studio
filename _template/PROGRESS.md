# PROGRESS — [НАЗВАНИЕ]

Единый трекер проекта (чеклист + лог + версии). Обновлять по ходу вместе с `CLAUDE.md` проекта.

## Чеклист
- [ ] 1. ТЗ + факты с сайта клиента (не выдумывать)
- [ ] 2. Сценарий hook→message→CTA + полные тексты с таймингами
- [ ] 3. Озвучка → `/timing public/voice.mp3` → `durationInFrames` в `Root.tsx` (+ `--captions` для соцсетей)
- [ ] 4. Ассеты: `/inventory` + общая `_ASSETS/broll/` (`inventory.py --broll`) → переиспользовать; догрузка `/broll "query"` (Pexels/Pixabay) → копировать в `public/` + 30fps
- [ ] 5. Музыка 3 варианта (corporate/cinematic/upbeat) + sfx → `public/`
- [ ] 6. Сцены `src/scenes/*.tsx` (движок `../kit`, процесс — навык `video-pipeline`)
- [ ] 7. Превью: `/render preview` → `/sheet` (обзор всего ролика) → правки до ≥9.5
- [ ] 8. Storyboard клиенту ДО финального рендера
- [ ] 9. Финал: `/render final` → `/mix` (голос+музыка) → `/preflight`
- [ ] 10. Отдать 3 версии `out/v1_*`; правки → `v2_*` (v1 не перезаписывать)

## Лог
- [дата] — начат проект.

## Версии
- v1 ([дата]) — первая версия, отдано клиенту.

## Очередь правок
1. [приоритет] …
